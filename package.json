{
  "name": "belljar-manager",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && npx prisma generate --schema=server/prisma/schema.prisma",
    "start": "npx prisma migrate deploy --schema=server/prisma/schema.prisma && node server/src/index.js"
  }
}
